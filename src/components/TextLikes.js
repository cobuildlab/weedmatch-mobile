import React from 'react';
import { Text } from 'react-native';

const TextLikes = ({ children }) => {

    return (
        <Text>
            {children}
        </Text>
    );
};

export default TextLikes;
